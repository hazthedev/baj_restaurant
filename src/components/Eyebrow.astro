---
interface Props {
  children: any;
  className?: string;
}

const { className = '' } = Astro.props;
---

<p class={`uppercase tracking-widest text-slate text-sm font-medium ${className}`}>
  <slot />
</p>
